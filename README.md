El archivo .env se debe configurar de la siguiente manera:

MONGOOSE_ENV = conexion al servidor MONGO_atlas con la coleccion normalizr.
MONGOURL_ENV = conexion al servidor MONGO_atlas con la coleccion passport.
